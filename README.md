# ScrapervWNs
Web novel scraper for freewebnovel.com

## Setup
pip install -r requirements.txt

## Usage
Edit the NOVELS list in scraper.py with your novel URLs, then run:
python scraper.py

Output is saved to the /output folder as .txt files.
